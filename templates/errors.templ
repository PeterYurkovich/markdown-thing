package templates

templ Error(code int, message string) {
    <link rel="stylesheet" href="/static/css/tailwind.css" />
    <div class="text-center text-orange-500 text-3xl">{string(rune(code)) + ": " + message}</div>
}
